<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miqroera.biosensor.domain.mapper.SysUserMapper">

    <!-- 更新用户首次检测日期和累计检测次数 -->
    <update id="updateUserMeasureInfo">
        UPDATE sys_user
        SET first_measure_date = COALESCE(first_measure_date, #{measureDate}),
            total_measures = COALESCE(total_measures, 0) + 1
        WHERE id = #{userId}
    </update>
    <select id="selectNickNameById" resultType="java.lang.String">
        SELECT nick_name
        FROM sys_user
        WHERE id = #{userId}
    </select>

</mapper>
